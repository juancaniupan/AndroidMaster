package com.compileit.androidmaster

fun main(){
    val weekDay = arrayOf("Lunes", "Martes", "Miercoles", "Jueves", "Viernes", "Sábado", "Domingo")

    for(i in weekDay.indices)
    {
        println(weekDay[i])
    }
}